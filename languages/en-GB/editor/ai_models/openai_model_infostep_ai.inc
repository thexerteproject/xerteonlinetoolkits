<?php
//standard group though this doesn't work for decision trees
    define("LEARNING_PROMPT_INFOSTEP", "Generate an interactive decision tree with the following description: an educational interactive story meant to educate children about talking to strangers. The story depth should be at least two steps, and there should be at least two possible results. Also, add in a place for the node which 'lead' us to this new infoStep. Do this in an xml format.");
    define("DEFAULT_PROMPT_INFOSTEP", "Using the exact same xml layout generate a infoStep node (keep it wrapped in the <decision> node!) for the following question: ,infoStep_content, . Make sure the name attribute of the infoStep node is set to ,infoStep_name, . For this node, don't add any linkIDs, only the name and make sure to not use any paragraph tags for the text!
    
    Within the decision node, add another childnode which corresponds to the existing node with the name ,infoStep_follow, ; The information for this node can be found in one of my previous messages with the context. Use the appropriate construction (either for the infoStep or mcqStep) similar to what you had in your previous message. Add the linkID attribute which is attached to that name, as well as the targetNew attribute which should be ,infoStep_name, . ,additionalInstructions, Don't add any other attributes, such as the name attribute itself or the text attribute.");
    define("LEARNING_RESULT_INFOSTEP", "<decision><mcqStep linkID = ''><mcqStepOption linkID = '' targetNew='info1'></mcqStep><infoStep linkID = '' targetNew='info1'/><infoStep name=\"info1\" text=\"Always be aware when talking to people you don't know! Make sure to double-check with your parent or minder before following the advice of a stranger, even if they are an adult!\"></infoStep></decision>");