<?php

    define("LEARNING_PROMPT_TEXTGRAPHICS", "Generate a page with some text for the following subjectg: fruits, in xml.");
    define("DEFAULT_PROMPT_TEXTGRAPHICS", "Use the same xml layout exactly and generate a <textGraphics> node which contains a single CDATA child node. The CDATA node should contain information about ,subject, . ,additionalInstructions, . Use this text as an example for the content in the CDATA node ,exampleContent, . If the example is blank, just ignore it. As much as possible, try to create the content in the following tone/style/voice: ,tone, . In terms of complexity of language, adapt the response to people of the following age range/education level/language proficiency: ,range, . In terms of difficulty of the content, please try as much as possible to create content which is appropriately CHALLENGING for people with the following background/education level: ,eduLevel, . Return only the XML. The XML should consist of just the <textGraphics> node and the CDATA child-node within which contains the information. The returned xml must be one line with no characters denoting new lines or paragraphs. The returned xml must be one line with no characters denoting new lines or paragraphs.");
    define("LEARNING_RESULT_TEXTGRAPHICS", "<textGraphics><![CDATA[Enter text for the page here]]></textGraphics>");
