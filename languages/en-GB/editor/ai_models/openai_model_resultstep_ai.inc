<?php
//standard group though this doesn't work for decision trees
define("LEARNING_PROMPT_RESULTSTEP", "Generate an interactive decision tree with the following description: an educational interactive story meant to educate children about talking to strangers. The story depth should be at least two steps, and there should be at least two possible results. Also, add in a place for the node which 'lead' us to this new resultStep. Do this in an xml format.");
define("DEFAULT_PROMPT_RESULTSTEP", "Using the exact same xml layout generate a resultStep node (keep it wrapped in the <decision> node!) for the following question: ,resultStep_content, . Make sure the name attribute of the resultStep node is set to ,resultStep_name, . For this node, don't add any linkIDs, only the name and make sure to not use any paragraph tags for the text!
    
    Within the decision node, add another childnode which corresponds to the existing node with the name ,resultStep_follow, ; The information for this node can be found in one of my previous messages with the context. Use the appropriate construction (either for the infoStep or mcqStep) similar to what you had in your previous message. Add the linkID attribute which is attached to that name, as well as the targetNew attribute which should be ,resultStep_name, . Don't add any other attributes, such as the name attribute itself or the text attribute.");
define("LEARNING_RESULT_RESULTSTEP", "<decision><mcqStep linkID = ''><mcqStepOption linkID = '' targetNew='result1'></mcqStep><infoStep linkID = '' targetNew='result1'/><resultStep name=\"result1\" text=\"Congratulations, you successfully navigated the situation and ended up having icecream with your mother!\"></resultStep></decision>");